fast-rlm==0.1.18
PyYAML==6.0.3
python-dotenv==1.2.2
pydantic==2.12.5
mcp==1.23.3

# Phase 2 build->verify->render stage (native; install in the geometry_server env)
cadquery
meshlib
matplotlib
trimesh
