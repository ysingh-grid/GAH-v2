Understand the multi tools calling and experiment with the different situations 
Understand the use of skills , use it for the different situations 
Trace everything like the subagents calls with what tools and skills does it is getting the right tools and skills 
come to multi level system , what it means is , we have something to do at layer A and whole another set of task at Layer 2 , how we can do that 


"Generate a list of 5 common fruits (make sure at least two of them have 8 or more characters in their names, e.g., Strawberry, Pomegranate, Blueberry, Elderberry).\n"
        "For each fruit, perform these operations:\n"
        "  1. Count the number of times the letter 'a' appears in its name (case-insensitive).\n"
        "  2. Use the 'calculate_string_length' tool to find the character length of the name.\n"
        "  3. If the name length is 8 or more characters, call the 'count_vowels_in_long_word' tool and record the count under 'vowel_count'.\n"
        "  4. If the name length is less than 8 characters, DO NOT call the vowel counting tool, and set 'vowel_count' to null.\n"
        "  5. Record all tool calls executed in the 'tools_used' dictionary (e.g., {'length_tool': 'calculate_string_length', 'vowel_tool': 'count_vowels_in_long_word'}).\n"
        "Return the final array matching the output schema structure."