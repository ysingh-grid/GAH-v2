# Available Skills (Replanner)

Use `read_skill(name)` to load any skill file.

**Start here:** read `playbook_replan` FIRST — it gives your role, tools, steps, and output contract for revising ONE existing plan.

- `playbook_replan`   ← read first, every replan
- `repair_guidance`
- `refinement_guidance`
- `primitive_planning`
- `dimension_reasoning`
